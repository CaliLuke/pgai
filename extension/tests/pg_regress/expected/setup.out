-- this setup file is run immediately after the regression database is (re)created
-- pgvector must be created first since our extension depends on it
CREATE EXTENSION vector;
CREATE EXTENSION extension;
